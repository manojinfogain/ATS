import { LibDateFilterPipe } from './lib-date-filter.pipe';

describe('DateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
