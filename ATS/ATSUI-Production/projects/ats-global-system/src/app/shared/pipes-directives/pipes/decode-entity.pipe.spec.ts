import { DecodeEntityPipe } from './decode-entity.pipe';

describe('DecodeEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
