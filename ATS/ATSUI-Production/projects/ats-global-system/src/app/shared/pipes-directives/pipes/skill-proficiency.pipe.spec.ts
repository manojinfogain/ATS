import { SkillProficiencyPipe } from './skill-proficiency.pipe';

describe('SkillProficiencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillProficiencyPipe();
    expect(pipe).toBeTruthy();
  });
});
