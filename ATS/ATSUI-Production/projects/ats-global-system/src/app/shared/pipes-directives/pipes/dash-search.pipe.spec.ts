import { DashSearchPipe } from './dash-search.pipe';

describe('DashSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DashSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
